@model IEnumerable<MVC_Store.Models.ViewModels.CategoryViewModel>

<a class="list-group-item list-group-item-primary">Categories</a>

@foreach (var item in Model)
{
    <a class="list-group-item list-group-item-action" href="/shop/category/@item.Slug">@item.Name</a>
}